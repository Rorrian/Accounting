export const enum Size {
  Big = 'big',
  Small = 'small',
}

export const enum Kind {
  Primary = 'primary',
  Secondary = 'secondary',

  Transparent = 'transparent',
}

export const enum Justify {
  Start = 'start',
  Center = 'center',
}
